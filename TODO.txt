我在写一个可视化的SSD垃圾回收（GC）模拟器，
【目录结构】
demo/
├── index.html          # 主入口（精简后的HTML）
├── styles.css          # 样式文件
├── ssd-gc-simulator.html # 原文件（保留备份）
└── js/
    ├── config.js       # 配置管理模块
    ├── state.js        # 状态管理模块
    ├── utils.js        # 工具函数模块
    ├── writeStrategy.js    # 写入策略模块
    ├── gcStrategy.js       # GC策略模块
    ├── renderer.js     # 渲染模块
    ├── app.js          # 前端交互模块
    └── main.js         # 入口组装模块

需求：
1. 触发GC时的弹窗现在不能拖动，现在我需要改为能够拖动；
2. 显示弹窗时，主界面UI变暗，我需要减小变暗的程度，提高主界面可见度。